# Pc_clicker 使用指导

Pc_clicker 是一个 Windows 下的鼠标/键盘自动化点击器，支持「鼠标单点循环」和「预编辑脚本循环」两种模式。

## 一、界面说明

主界面顶部菜单栏有三个按钮：

| 按钮 | 说明 |
| --- | --- |
| 单点循环 | 在鼠标当前位置反复点击指定按键（可设置点击间隔），不需要脚本文件 |
| 配置循环 | 按预编辑的脚本文件执行点击、按键、等待操作 |
| 指导文档 | 打开本文件 |

菜单栏下方是循环类型选择：**次数循环**、**时间循环(分钟)**、**无限循环**，以及对应的次数/时间输入框（只能输入数字，切换循环类型时会重置为 1）。输入框文字说明固定为「循环次数/时间」，三种模式共用。

界面底部的两个按钮：

| 按钮 | 说明 |
| --- | --- |
| 鼠标位置 | 显示/关闭跟随鼠标的坐标浮窗，浮窗显示鼠标在目标内的相对比例 0-1（仅在「配置循环」界面可用） |
| 执行 | 开始/停止循环，执行过程中按钮文字变为「停止」。单点循环点「执行」后会先等 1 秒再开始，方便把鼠标移到目标位置 |

**快捷键**

| 快捷键 | 说明 |
| --- | --- |
| Ctrl + 小键盘 0 | 开始 / 停止执行（全局有效；单点循环推荐用它开始，鼠标不必停在按钮上） |
| Ctrl + 小键盘 . | 显示 / 关闭鼠标坐标浮窗（全局有效，仅在「配置循环」界面有效） |

> 两个快捷键都需要**开启 NumLock**（关闭时小键盘 0/. 会变成 Ins/Del，快捷键不生效），并且左右 Ctrl 都可以。

## 二、循环模式说明

| 循环类型 | 说明 |
| --- | --- |
| 次数循环 | 按输入的整数次数执行脚本 |
| 时间循环(分钟) | 输入的分钟数是**总循环时长**，程序会在这段时间内不断重复执行脚本，时间到自动停止 |
| 无限循环 | 一直执行脚本，直到按 Ctrl+小键盘0 或点击「停止」 |

执行前程序会检查：

1. 脚本第一行是否与当前选择的目标名称一致，不一致会弹窗提示并停止执行（请修改脚本后重试）；
2. 每一行操作是否符合规范，不符合会弹窗提示出错的**行号**并停止执行。

## 三、鼠标位置浮窗

在「配置循环」界面点击「鼠标位置」会弹出跟随鼠标的小浮窗，共两行：

- 第一行：鼠标相对当前所选目标（窗口客户区或屏幕）的**相对比例 0-1**，可直接填入脚本的 `x`、`y`；鼠标不在目标范围内时显示「超出范围」；
- 第二行：当前目标（进程文件名或「屏幕x」）。

浮窗只是显示用，不接收鼠标操作（鼠标点击会直接穿过它，也不会抢焦点），始终跟随鼠标、不会压住鼠标，鼠标移到屏幕右侧或下侧边缘时会自动翻到另一侧。显示/关闭方式：快捷键 **Ctrl+小键盘.** 或主界面下方的「鼠标位置」按钮（两者效果相同）。

## 四、脚本文件

脚本文件保存在程序目录下的 `scripts` 文件夹中，扩展名为 `.txt`，可以直接用记事本编辑。

界面上的脚本列表（ListView）显示的是**转换后的中文描述**（例如「目标：屏幕1」「点击左键 (50%, 25%)」「等待 500 毫秒」），实际文件内容仍是下面的脚本格式。

文件**第一行**必须填写本次操作的目标名称，用于执行前的检查：

- 选择的是某个窗口时：填写**进程文件名**（如 `notepad.exe`、`msedge.exe`，即「目标程序」下拉框中方括号 `[0x...]` 前面的文字）。用进程文件名而不是窗口标题，是因为窗口标题会随程序状态变化（如浏览器标签页）而失效；
- 选择的是某块显示器时：填写 **`screen1`**、`screen2`……（与「目标程序」下拉框中的屏幕顺序一致）。脚本里统一使用英文标识，界面上显示为「屏幕1」，便于以后做多语言适配。

第一行之后，**每一行记录一个操作**，格式为：`指令类型 + 空格 + 参数`。指令类型有三种：`mouse`（点击）、`key`（按键）、`wait`（等待）；空行会被忽略。

### 1. 点击操作 mouse

```
mouse 按键编号+x坐标+y坐标
```

| 参数 | 说明 |
| --- | --- |
| 按键编号 | `0` 左键、`1` 右键、`2` 中键、`3` 前侧键、`4` 后侧键 |
| x坐标 / y坐标 | 点击位置，**0-1 的相对比例**（`0.5` 表示一半处）；填写 `-1+-1` 表示不移动鼠标，直接在鼠标当前位置点击 |

例如 `mouse 0+0.5+0.25` 表示左键点击目标区域横向一半、纵向四分之一的位置。参数用空格分隔（`mouse 0 0.5 0.25`）也可以，但推荐统一使用 `+` 连接。

相对比例的基准（不管目标窗口多大、分辨率多高，脚本都不用改）：

- 目标为窗口时：窗口客户区的左上角为 `0`，右下角为 `1`；
- 目标为屏幕时：该显示器左上角为 `0`，右下角为 `1`。

用界面上的「鼠标位置」浮窗可以实时查看当前鼠标对应的相对比例，把它填进脚本即可。

### 2. 按键操作 key

```
key 按键表达式
```

按键表达式支持单个按键，也支持用 `+` 连接的组合键，例如：

```
key a
key shift+a
key ctrl+alt+del
key win+r
key menu
key f5
key enter
```

### 3. 等待操作 wait

```
wait 毫秒数
```

例如 `wait 500` 表示等待 500 毫秒（即 0.5 秒）。

### 4. 示例脚本

```
notepad.exe
mouse 0+0.3+0.25
wait 300
key ctrl+a
wait 100
key ctrl+c
wait 200
mouse 1+0.7+0.6
```

上面的脚本表示：先左键点击窗口内容区域横向 30%、纵向 25% 的位置，等待 0.3 秒，按下 Ctrl+A，等待 0.1 秒，按下 Ctrl+C，等待 0.2 秒，再右键点击横向 70%、纵向 60% 的位置。

## 五、按键名称表

字母键（`a`~`z`）和数字键（`0`~`9`）直接写即可。其它按键名称如下（不区分大小写）：

| 按键名称 | 对应按键 | 按键名称 | 对应按键 |
| --- | --- | --- | --- |
| `ctrl` / `control` | Ctrl（默认左 Ctrl，写 `lctrl`/`rctrl` 可指定左右） | `esc` / `escape` | Esc |
| `alt` | Alt（默认左 Alt，写 `lalt`/`ralt` 可指定左右） | `space` | 空格 |
| `shift` | Shift（默认左 Shift，写 `lshift`/`rshift` 可指定左右） | `backspace` / `back` | 退格 |
| `win` | Win（默认左 Win，写 `lwin`/`rwin` 可指定左右） | `del` / `delete` | Delete |
| `menu` / `apps` / `contextmenu` | 菜单键（唤起右键菜单，与 Alt 不同） | `ins` / `insert` | Insert |
| `tab` | Tab | `home` / `end` | Home / End |
| `enter` / `return` | 回车 | `pageup` / `pagedown` | PageUp / PageDown |
| `up` / `down` / `left` / `right` | 方向键 | `capslock` | 大写锁定 |
| `numlock` | 数字锁定 | `scrolllock` | 滚动锁定 |
| `printscreen` | PrintScreen | `pause` / `break` | Pause/Break |
| `f1` ~ `f12` | 功能键 |  |  |

小键盘：

| 按键名称 | 对应按键 | 按键名称 | 对应按键 |
| --- | --- | --- | --- |
| `num0` ~ `num9` | 小键盘数字 | `numadd` / `numplus` | 小键盘 + |
| `numsubtract` / `numminus` | 小键盘 - | `nummultiply` / `numstar` | 小键盘 * |
| `numdivide` / `numslash` | 小键盘 / | `numdecimal` / `numdot` | 小键盘 . |
| `numenter` | 小键盘回车 | `numclear` / `numseparator` | 小键盘 清除/分隔符 |

多媒体与音量：

| 按键名称 | 对应按键 | 按键名称 | 对应按键 |
| --- | --- | --- | --- |
| `volumeup` / `volup` | 音量 + | `volumedown` / `voldown` | 音量 - |
| `volumemute` / `volmute` / `mute` | 静音 | `mediaplaypause` / `playpause` / `play` | 播放/暂停 |
| `medianext` / `nexttrack` / `next` | 快进（下一曲） | `mediaprev` / `prevtrack` / `prev` | 快退（上一曲） |
| `mediastop` | 停止播放 | `launchmedia` / `mediaselect` | 打开媒体 |

浏览器与其它：

| 按键名称 | 对应按键 | 按键名称 | 对应按键 |
| --- | --- | --- | --- |
| `browserback` | 后退 | `browserforward` | 前进 |
| `browserrefresh` | 刷新 | `browserstop` | 停止加载 |
| `browsersearch` | 搜索 | `browserfavorites` | 收藏夹 |
| `browserhome` | 主页 | `launchmail` | 打开邮件 |
| `launchapp1` / `launchapp2` | 应用程序键 1/2 | `sleep` | 睡眠 |

> 组合键请用小写 `+` 连接，例如 `ctrl+alt+del`、`shift+a`、`win+r`；`menu` 是独立的菜单键（VK_APPS），不等于 `alt`。

## 六、注意事项

1. 使用脚本循环时鼠标会被程序控制，请按 **Ctrl+小键盘0** 停止；脚本界面顶部也有相同提示。
2. 执行窗口类脚本时，程序会先把目标窗口切到前台，再按相对比例点击。脚本运行期间请不要移动或缩放目标窗口。
3. 窗口标题会随程序状态变化，但脚本首行记录的是**进程文件名**，因此标题变化不影响脚本执行。
4. 目标窗口被关闭时，脚本会自动停止并提示。
5. 单点循环不读取脚本文件，直接在鼠标当前位置点击，此界面下「鼠标位置」按钮不可用。因为「执行」按钮就在鼠标底下，点「执行」后会延时 1 秒再开始，建议把鼠标先移到目标位置（或直接用 Ctrl+小键盘0 开始）。
6. 单点循环界面最下方有「点击间隔(毫秒)」输入框（默认 50，只能填数字），表示每次点击之间等待的时间；填 0 表示连续点击。
