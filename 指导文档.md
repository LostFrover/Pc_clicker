# Pc_clicker 使用指导

Pc_clicker 是一个 Windows 下的鼠标/键盘自动化点击器，支持「鼠标单点循环」和「预编辑脚本循环」两种模式。

## 一、界面说明

主界面顶部菜单栏有三个按钮：

| 按钮 | 说明 |
| --- | --- |
| 单点循环 | 在鼠标当前位置反复点击指定按键，不需要脚本文件 |
| 配置循环 | 按预编辑的脚本文件执行点击、按键、等待操作 |
| 指导文档 | 打开本文件 |

菜单栏下方是循环类型选择：**次数循环**、**时间循环(分钟)**、**无限循环**，以及对应的次数/时间输入框（只能输入数字，切换循环类型时会重置为 1）。

界面底部的两个按钮：

| 按钮 | 说明 |
| --- | --- |
| 鼠标位置 | 显示/关闭跟随鼠标的坐标浮窗（仅在「配置循环」界面可用，F9） |
| 执行 | 开始/停止循环，执行过程中按钮文字变为「停止」（F8） |

**快捷键**

| 快捷键 | 说明 |
| --- | --- |
| F8 | 开始 / 停止执行（全局有效，脚本运行时可直接按 F8 停止） |
| F9 | 关闭鼠标坐标浮窗 |
| Esc | 关闭鼠标坐标浮窗（浮窗获得焦点时） |

## 二、脚本文件

脚本文件保存在程序目录下的 `scripts` 文件夹中，扩展名为 `.txt`，可以直接用记事本编辑。

文件**第一行**必须填写本次操作的目标名称，用于执行前的检查：

- 选择的是某个窗口时：填写该窗口的标题（即「目标程序」下拉框中方括号 `[0x...]` 前面的完整文字）；
- 选择的是某块显示器时：填写「屏幕1」「屏幕2」……（与「目标程序」下拉框中显示的屏幕名一致）。

第一行之后，**每一行记录一个操作**，格式为：`指令类型 + 空格 + 参数`。指令类型有三种：`mouse`（点击）、`key`（按键）、`wait`（等待）；空行会被忽略。

### 1. 点击操作 mouse

```
mouse 按键类型 x y
```

| 参数 | 说明 |
| --- | --- |
| 按键类型 | `0` 左键、`1` 右键、`2` 中键、`3` 前侧键、`4` 后侧键 |
| x y | 点击位置，**相对坐标**（见下方说明）；填写 `-1 -1` 表示不移动鼠标，直接在鼠标当前位置点击 |

相对坐标的基准：

- 目标为窗口时：相对窗口客户区左上角（即窗口内容区域的左上角）；
- 目标为屏幕时：相对该显示器的左上角。

用界面上的「鼠标位置」浮窗可以实时查看当前鼠标对应的相对坐标，把它填进脚本即可。

### 2. 按键操作 key

```
key 按键表达式
```

按键表达式支持单个按键，也支持用 `+` 连接的组合键，例如：

```
key a
key shift+a
key ctrl+alt+del
key win+r
key menu
key f5
key enter
```

### 3. 等待操作 wait

```
wait 毫秒数
```

例如 `wait 500` 表示等待 500 毫秒（即 0.5 秒）。

### 4. 示例脚本

```
记事本
mouse 0 120 80
wait 300
key ctrl+a
wait 100
key ctrl+c
wait 200
mouse 1 400 300
```

上面的脚本表示：先点击窗口内的 (120, 80)，等待 0.3 秒，按下 Ctrl+A，等待 0.1 秒，按下 Ctrl+C，等待 0.2 秒，再右键点击 (400, 300)。

## 三、按键名称表

字母键（`a`~`z`）和数字键（`0`~`9`）直接写即可。其它按键名称如下（不区分大小写）：

| 按键名称 | 对应按键 | 按键名称 | 对应按键 |
| --- | --- | --- | --- |
| `ctrl` / `control` | Ctrl | `esc` / `escape` | Esc |
| `alt` / `menu` | Alt | `space` | 空格 |
| `shift` | Shift | `backspace` / `back` | 退格 |
| `win` / `lwin` | Win | `del` / `delete` | Delete |
| `tab` | Tab | `ins` / `insert` | Insert |
| `enter` / `return` | 回车 | `home` / `end` | Home / End |
| `up` / `down` / `left` / `right` | 方向键 | `pageup` / `pagedown` | PageUp / PageDown |
| `capslock` | 大写锁定 | `numlock` | 数字锁定 |
| `scrolllock` | 滚动锁定 | `printscreen` | PrintScreen |
| `f1` ~ `f12` | 功能键 | `num0` ~ `num9` | 小键盘数字键 |

> 注意：`alt` 与 `menu` 都表示 Alt 键；组合键请用小写 `+` 连接，例如 `ctrl+alt+del`。

## 四、循环模式说明

| 循环类型 | 说明 |
| --- | --- |
| 次数循环 | 按输入的整数次数执行脚本 |
| 时间循环(分钟) | 每隔输入的分钟数执行一次脚本，直到手动停止 |
| 无限循环 | 一直执行脚本，直到按 F8 或点击「停止」 |

执行前程序会检查：

1. 脚本第一行是否与当前选择的目标名称一致，不一致会弹窗提示并停止执行（请修改脚本后重试）；
2. 每一行操作是否符合规范，不符合会弹窗提示出错的**行号**并停止执行。

## 五、鼠标位置浮窗

在「配置循环」界面点击「鼠标位置」会弹出跟随鼠标的浮窗，显示：

- **鼠标位置坐标**：相对当前所选目标（窗口客户区或屏幕）的坐标，可直接填入脚本的 `x y`；
- **屏幕坐标**：鼠标的绝对屏幕坐标。

浮窗可通过「关闭」按钮、F9 或 Esc 关闭。鼠标移到屏幕右侧或下侧边缘时，浮窗会自动翻到另一侧，避免被遮挡。

## 六、注意事项

1. 使用脚本循环时鼠标会被程序控制，请按 **F8** 停止；脚本界面顶部也有相同提示。
2. 执行窗口类脚本时，程序会先把目标窗口切到前台，再按相对坐标点击。脚本运行期间请不要移动或缩放目标窗口。
3. 窗口标题会随程序状态变化（如浏览器标签页标题），若第一行与目标标题不一致，请修改脚本第一行后再执行。
4. 目标窗口被关闭时，脚本会自动停止并提示。
5. 单点循环不读取脚本文件，直接在鼠标当前位置点击，此界面下「鼠标位置」按钮不可用。
