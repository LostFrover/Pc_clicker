# 项目目标
编写一个windows系统下的点击器程序，能够自动化执行鼠标点击操作和键盘输入操作。  

### UI设计：
目前UI的基本结构已经在UI文件夹的.ui文件中完成，后续需要以此qt的ui文件为基础进行wpf界面的开发。
1. **主界面**  
   1. 标题框下方有一行菜单栏，用于切换鼠标单点循环和预编辑脚本循环两个子界面，以及打开指导文档，共3个按钮。
   2. 正中是两个子界面的父组件，默认显示鼠标单点循环界面。
   3. 再往下是一个循环模式的选择radiobutton，用户可以选择循环模式为“无限循环”、“次数循环”、“定时循环”，并在旁边的输入框中输入循环次数，这个输入框限制只能输入数字，并且是次数/时间共用，切换时需要重置这个输入框为1。
   4. 最下方有鼠标位置显示按钮和执行按钮，分别用于创建显示鼠标位置并跟随的气泡浮窗和开始执行循环。
   
2. **鼠标单点循环界面**
   1. 单点界面包含一个介绍label和切换鼠标按键的radiobutton组，按键包括左键、右键、中键、侧键1、侧键2，默认选择左键
   2. 使用此界面时，鼠标位置显示按钮disable
   3. 功能相关的补充：
       1. 单点循环的实现应当和预编辑脚本使用同一个方法，只是在单点循环界面里，不需要读取脚本文件，在界面类里预设参数，点击时，不移动鼠标直接执行鼠标点击操作。

3. **预编辑脚本循环界面**
   1. 界面顶部一个介绍label
   1. 第二行是选择目标进程的combobox，点击后查找当前系统中正在运行的用户进程，并展示窗口标题和窗口句柄，用户选择其中一个进程，程序会记录该进程的窗口句柄，后续点击时使用该窗口句柄和相对位置进行鼠标点击操作。固定添加连接的所有显示器在最前列，记为屏幕x，选择这些项后操作时不使用窗口句柄，直接使用鼠标在该屏幕的相对坐标进行点击操作。
   2. 之后是选择目标脚本文件控件组，包括选择文件的combobox和新增、删除、编辑、打开脚本文件夹的按钮
   3. 再向下是展示脚本内容的listview，用户可以在这里查看脚本的每一行内容
   4. 功能相关的补充：
       1. combobox在下拉时再读取脚本文件夹下的所有脚本文件，显示在下拉列表中，用户选择其中一个脚本文件
       2. 点击新增按钮，打开一个命名弹窗，输入文件名，，之后新建一个脚本文件
       3. 点击删除按钮删除当前选中的脚本文件
       4. 点击编辑按钮通过记事本打开目标脚本文件，可以编辑当前选中的脚本文件，打开后需要弹窗提醒用户编辑并保存后再关闭该提醒弹窗，并在关闭提醒弹窗后重新读取脚本文件内容到listview中

### 功能点：
1. **脚本内容**      
    1. 脚本首行记录目标程序的窗口标题，后续点击时使用窗口的相对位置，之后每一行记录一个操作，操作包括鼠标点击、键盘按键和等待操作。
    2. 各类操作以一个操作一行的形式保存到配置文件，文件使用ini格式，具体结构为：'指令类型'+'空格'+'参数'，其中指令类型有三种：点击、按键、等待。  
        1. 点击操作：
            1. 指令类型：mouse
            2. 参数：鼠标按键类型（左键-0、右键-1、中键-2、侧键-3/4）、鼠标点击的相对位置（（-1 -1）代表鼠标当前位置）
        2. 按键操作：
            1. 指令类型：key
            2. 参数 这次操作的全部按键（例如：menu、ctrl+alt+del、shift+a、win+r等），后续编写指导时，需要在指导文档中列出非字母/数字键的按键名称
        3. 等待操作：
            1. 指令类型：wait
            2. 参数：等待时间（单位为毫秒/千分秒）
     3. 执行前检查脚本文件的首行是否与当前选择的目标进程窗口标题一致，如果不一致，弹窗提示用户需要修改并停止执行；之后检查每一行的操作是否符合规范，如果不符合规范，弹窗提示用户需要修改并停止执行。

2. **循环模式**  
   1. 无限循环：程序会一直执行脚本，直到用户手动停止
   2. 次数循环：用户可以输入一个整数，程序会执行指定次数的脚本
   3. 定时循环：用户可以输入一个时间间隔（单位为分钟，整数），程序会每隔指定时间执行一次脚本
   4. 点击执行按钮或快捷键进行开始/停止操作，执行过程中，执行按钮文字跟随执行状态变更

3. **鼠标位置显示**     
   1. 根据子界面选择的目标进程（选择屏幕x的情况使用此屏幕的相对位置，单点循环时功能禁用），程序会获取该进程的窗口句柄，并在点击鼠标位置显示按钮后，创建一个气泡浮窗，显示当前鼠标的坐标位置，并且浮窗会跟随鼠标移动
   1. 点击鼠标位置显示按钮后，程序会创建一个气泡浮窗，显示当前鼠标的坐标位置，并且浮窗会跟随鼠标移动
   2. 浮窗可以通过点击关闭按钮或快捷键关闭
   3. 在屏幕右/下边缘时，浮窗自动调整位置，避免被遮挡
   4. 浮窗显示内容为：鼠标位置坐标:x,y